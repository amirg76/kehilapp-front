# Copy to .env and set for your environment. The app fail-fasts (white screen)
# if VITE_REACT_APP_BASE_URL is missing, so it must be present.
# Points at the backend API.
VITE_REACT_APP_BASE_URL=http://localhost:5001
